// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    applicationId = "com.labsnip.azzahra"
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
